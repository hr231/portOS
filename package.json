{
  "name": "harshit-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "PYTHONPATH=. uvicorn backend.main:app --reload --port 8000",
    "ingest": "PYTHONPATH=. python3 -m pipeline.ingest",
    "reindex": "PYTHONPATH=. python3 -m pipeline.ingest --force"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
